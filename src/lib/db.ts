
// No longer needed - this file is now deprecated as we've moved all persistence to Supabase.
export const openDB = () => { throw new Error("use Supabase instead of indexedDB"); };
export const addItem = async () => { throw new Error("use Supabase instead of indexedDB"); };
export const getAllItems = async () => { throw new Error("use Supabase instead of indexedDB"); };
export const getItemById = async () => { throw new Error("use Supabase instead of indexedDB"); };
export const updateItem = async () => { throw new Error("use Supabase instead of indexedDB"); };
export const deleteItem = async () => { throw new Error("use Supabase instead of indexedDB"); };
export const queryByIndex = async () => { throw new Error("use Supabase instead of indexedDB"); };
